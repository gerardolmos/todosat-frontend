---
import { getMediaUrl } from "../lib/media";

interface Props {
    marca: any;
}

const { marca } = Astro.props;

const logoUrl = getMediaUrl(marca.logo);
const portadaUrl = getMediaUrl(marca.imagen_portada);
---

<a
    href={`/marcas/${marca.slug}`}
    class="group block overflow-hidden rounded-2xl border border-brand-border bg-white shadow-sm transition hover:-translate-y-1 hover:border-info-primary/40 hover:shadow-lg"
>
    {
        portadaUrl && (
            <div class="overflow-hidden bg-brand-soft">
                <img
                    src={portadaUrl}
                    alt={marca.nombre}
                    class="h-56 w-full object-cover transition duration-500 group-hover:scale-105"
                />
            </div>
        )
    }

    <div class="p-6">
        {
            logoUrl && (
                <img
                    src={logoUrl}
                    alt={`Logo de ${marca.nombre}`}
                    class="mb-5 max-h-12 w-auto"
                />
            )
        }

        <h3 class="text-2xl font-bold tracking-tight text-brand-base">
            {marca.nombre}
        </h3>

        {
            marca.descripcion_corta && (
                <p class="mt-3 text-sm leading-6 text-brand-muted">
                    {marca.descripcion_corta}
                </p>
            )
        }

        <p class="mt-5 text-sm font-semibold text-info-primary">Ver marca →</p>
    </div>
</a>
